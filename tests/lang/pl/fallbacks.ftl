in-current-locale-bundle = In PL bundle.
    .attr = Attribute in PL bundle.

# in-fallback-locale-bundle = In PL bundle.
#     .attr = Attribute in PL bundle.

# in-current-locale-php = In PL bundle.

# in-fallback-locale-php = In PL bundle.

choice = PHP translation file should be used for choice.
