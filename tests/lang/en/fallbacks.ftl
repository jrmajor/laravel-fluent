in-current-locale-bundle = In EN bundle.
    .attr = Attribute in EN bundle.

in-fallback-locale-bundle = In EN bundle.
    .attr = Attribute in EN bundle.

# in-current-locale-php = In EN bundle.

# in-fallback-locale-php = In EN bundle.
